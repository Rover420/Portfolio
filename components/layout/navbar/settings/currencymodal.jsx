import Modal from "@/components/external/modal"

const CurrencyModal = ({ setIsOpen }) => {
  return (
    <Modal setIsOpen={setIsOpen}>
        <div>asd</div>
    </Modal>
  )
}

export default CurrencyModal